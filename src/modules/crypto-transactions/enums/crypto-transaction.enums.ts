export enum CryptoCurrency {
    BTC = 'BTC',
    ETH = 'ETH',
    ALGO = 'ALGO',
    XLM = 'XLM',
    LTC = 'LTC',
}

export enum CryptoStatus {
    INITIATED = 'initiated',
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    FAILED = 'failed',
    CANCELLED = 'cancelled',
}
